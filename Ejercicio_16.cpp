#include <iostream>
using namespace std;
int main()
{
    int edad, mayoredad, juvenil;
    cin >> edad;
    mayoredad = (edad > 18);
    juvenil = (edad > 15) && (edad <= 18);
    cout << "";

    return 0;
}
